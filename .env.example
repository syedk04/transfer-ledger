# Copy this file to `.env` at the repo root (uvicorn backend.main:app loads
# it on startup via python-dotenv). Neither variable is actually read by any
# code yet - they're reserved for the agent/news-retrieval commits that come
# next, so the .env convention is in place before those land.

GROQ_API_KEY=
NEWSDATA_API_KEY=
