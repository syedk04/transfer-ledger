# Copy to .env.local for local dev (Vite loads .env.local automatically).
# The only backend URL the frontend knows - no provider keys live here,
# every Groq/NewsData call happens server-side.
VITE_API_BASE_URL=http://localhost:8000
